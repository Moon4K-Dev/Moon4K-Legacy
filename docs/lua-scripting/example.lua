-- example.lua
function onCreate()
    -- Called when the script is loaded
end


function onUpdate(elapsed)
    -- Called every frame
end

function onNoteHit(noteMs, judgment)
    -- Called when a note is hit
end 

function onNoteMiss(direction)
    -- Called when a note is missed
end

function onStep()
    -- Called every step
end

function onBeat()
    -- Called every beat
end